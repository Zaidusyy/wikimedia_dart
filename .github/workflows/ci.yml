name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    # Nightly at 04:17 UTC — catches upstream Wikimedia API contract drift.
    - cron: "17 4 * * *"
  workflow_dispatch:

jobs:
  test:
    name: Dart ${{ matrix.sdk }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    # Stable gates the build; beta is advisory and never fails the run.
    continue-on-error: ${{ matrix.sdk == 'beta' }}
    strategy:
      # Don't let one channel failing cancel the other.
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        sdk: [stable, beta]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Dart SDK
        uses: dart-lang/setup-dart@v1
        with:
          sdk: ${{ matrix.sdk }}

      - name: Install dependencies
        run: dart pub get

      # Formatting can differ between SDK versions, so gate on stable only.
      - name: Verify formatting
        if: matrix.sdk == 'stable'
        run: dart format --output=none --set-exit-if-changed .

      # Stable is the strict gate. Beta is advisory: it still fails on real
      # warnings/errors but not on new infos the pre-release analyzer may add.
      - name: Analyze (strict)
        if: matrix.sdk == 'stable'
        run: dart analyze --fatal-infos

      - name: Analyze (advisory)
        if: matrix.sdk == 'beta'
        run: dart analyze

      - name: Run tests
        run: dart test --coverage=coverage

      - name: Activate coverage tool
        if: matrix.sdk == 'stable'
        run: dart pub global activate coverage

      - name: Format coverage
        if: matrix.sdk == 'stable'
        run: |
          dart pub global run coverage:format_coverage \
            --lcov \
            --in=coverage \
            --out=coverage/lcov.info \
            --report-on=lib

      - name: Upload coverage to Codecov
        if: matrix.sdk == 'stable'
        uses: codecov/codecov-action@v4
        with:
          file: coverage/lcov.info
          fail_ci_if_error: false

  # Live integration tests against the real Wikimedia API. These are skipped
  # in the unit suite above (see dart_test.yaml) and run only on the nightly
  # schedule or a manual dispatch, so upstream contract drift is caught
  # without flaking pull-request CI.
  integration:
    name: Live integration tests
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Dart SDK
        uses: dart-lang/setup-dart@v1
        with:
          sdk: stable

      - name: Install dependencies
        run: dart pub get

      - name: Run integration tests
        run: dart test --run-skipped --tags integration

